name: Keep Server Alive

on:
  schedule:
    # רץ כל 10 דקות
    - cron: '*/10 * * * *'
  workflow_dispatch: # מאפשר הרצה ידנית לבדיקה

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render Backend
        run: curl -I https://tavili-api.onrender.com
